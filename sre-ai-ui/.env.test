# ─── Test-only dummy credentials ─────────────────────────────────────────────
# These are placeholder values for CI/testing ONLY — NOT real credentials.
# Sentinel scanner whitelists obvious dummy values; never put real data here.
PINGFED_CLIENT_ID=dummy
PINGFED_CLIENT_SECRET=dummy
PINGFED_REDIRECT_URI=http://localhost:3000/api/auth/callback
PINGFED_AUTH_URL=http://localhost/as/authorization.oauth2
PINGFED_TOKEN_URL=http://localhost/as/token.oauth2
PINGFED_USERINFO_URL=http://localhost/idp/userinfo.openid
PINGFED_LOGOUT_URL=http://localhost/as/logout
PINGFED_SCOPE=openid profile email

# ─── API Endpoints (test dummies) ────────────────────────────────────────────
SRE_OPERATOR_URL=http://localhost:9099
PROMQL_API_URL=http://localhost:9090
CHANGE_REQUESTS_API_URL=http://localhost:8030/api/change_requests
INCIDENTS_API_URL=http://localhost:8030/api/incidents
